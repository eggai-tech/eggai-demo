apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: redpanda-console
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
      sectionName: eggai-demo
  hostnames:
    - redpanda.eggai.localhost
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: redpanda-console
          port: 8080
